function makeTask(task) {
    const html = /*html*/`
        <h3 class="task" >
        </h3>
        <h3 class="${task.due ? Date.now : task.due}"> 
        </h3>
        
    `;    
const template = document
}
